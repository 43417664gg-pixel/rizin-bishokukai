# RIZIN美食会予想 — 仲間内勝敗予想ポータル

ハメ太郎・抜き太郎・丸山探偵事務所の3人でRIZINの勝敗を予想し合う非公開ポータル。

**最終更新：2026-08-18**（この日付より後のコミットがある場合、本書が古い可能性を疑うこと）

---

## いまの状態

| 項目 | 状態 |
|---|---|
| Supabase | **接続済み・本番稼働中**（project ref `rdlhhnbwjomlejszmkbv`） |
| データ（本番DB） | members 3／fighters **82**／events 5／fights **34**／predictions 28＋外部予想（influencer_sources.md 参照） |
| LANDMARK 15（7/18） | 結果発表済み（11試合） |
| RIZIN.54（8/11） | 結果発表済み（11試合） |
| 超RIZIN.5（9/10 京セラD） | **登録済み・全9試合・予想受付中**。9試合目＝ベイノア vs 宇佐美秀メイソンは**キックルールのため no_score（予想対象外）** |
| LANDMARK 16（10/3 長崎） | **カード発表済み・3試合を登録**（公式は「追加対戦カード決定次第」） |
| 11/8 の大会 | 公式の正式名称は **RIZIN.55**（LaLa arena TOKYO-BAY）。旧「LANDMARK 17 in CHIBA」は誤り。カード未発表 |
| 公開URL | https://43417664gg-pixel.github.io/rizin-bishokukai/ |

---

## ★次セッションへの引き継ぎ（2026-08-18）

### この回でやったこと（全て push 済み・本番反映＆実測確認済み）
- **キャッチアップを実施**。RIZIN公式と demo-data.js を突き合わせて差分を反映
  - 超RIZIN.5 に9試合目「“ブラックパンサー”ベイノア vs. 宇佐美秀メイソン」を追加。
    **RIZIN オープンフィンガーグローブ キックボクシングルール（3分3R・71.0kg）**のため
    `no_score: true` を付けて**予想対象外**で掲載（従来の「キック戦は掲載対象外」方針と
    Gakuの「載せて」の折衷。的中率に混ぜたければフラグを外すだけ）
  - LANDMARK 16 のカードが公式発表されていたので3試合を登録
  - 11/8 の大会名を **RIZIN.55** に修正（会場＝LaLa arena TOKYO-BAY）
  - 新規選手8人を名鑑へ（74→82人）。異名8件は `nickname_overrides.json` に出典付きで固定

### Gakuの確認が要る（蘭が暫定で決めたもの）
1. **異名4件は候補が複数あり、蘭が「Wikipedia infoboxの先頭」を暫定採用した**。表に出す通り名を確定してほしい
   - 堀江圭功＝THE RISING STAR（他：THE RISING FUTURE BOY／裸の銃をもつ漢／封印指定・特級呪物）
   - 宇佐美正パトリック＝THE SAVAGE（他：聖なる破壊者／怒れる狂拳）
   - 松嶋こよみ＝現代MMAの申し子（他：非RIZIN流 決意の出陣／謎日本人）
   - 芦澤竜誠＝BAD NAMAZU（他：進化する問題児／帰ってきた問題児／ペガサスの核弾頭）
2. **キック戦の扱い**（no_score のままにするか、通常の予想対象にするか）
3. **井上聖矢の戦績でソースが食い違う**。Wikipedia の MMA戦績表＝1勝1敗（KO1）／DATA MMA＝0勝1敗。
   情報源マップに従い Wikipedia を採用したが、履歴に出せる勝ち試合が無い状態

### 取れなかったもの（理由つき）
- **LANDMARK 16 のポスター**：公式の大会情報ページに縦長画像（B2比）が無い。
  og:image で代用すると横長のテキスト画像を掴むので、方針どおり取得を見送った。**Gakuの目視が要る**
- **ベイノアの試合履歴**：DATA MMA に該当ページが無い（主戦場がキックのため）。通算戦績と内訳は Wikipedia から取得済み
- **ケイト・ロータス vs NOEL の延期先**：公式が未発表。LM16 のカードページは「※追加対戦カード決定次第」のまま

### まだ残っている宿題
- **超RIZIN.5 の外部予想が0件**（RIZIN.54 は15本取り込み済み）。開幕前に `run_intake.py --event ev_cho5` を回すこと。母集団ゼロだと的中率ランキングが空回りする
- 異名が空の18人（Gaku確定待ち）／勝利内訳が無く円グラフが出ない7人
- 元王者13人＋αのプロフィールが薄い（履歴なし15人・ハイライト画像なし18人）
- スダリオ剛だけプロフィール全欠（所属・出身・生年月日・スタイル・来歴）
- ドンマイ川端の生写真（RIZIN初参戦のため超RIZIN.5後にしか素材が出ない＝待ちで正しい）

---

## ★前回の引き継ぎ（2026-08-03）

### Gakuの手が要る（未完了・最優先）
本番DBに列が無く、今セッションの成果（年齢・中止表示）が未反映。**1本のSQL → ⚡同期**で全部乗る。
1. Supabase SQL Editor で `supabase/migrations/2026-08-03_add_columns.sql` を実行
   （`fighters.birth_date` ／ `fights.cancelled` ／ `fights.cancel_note` を追加。冪等）
2. admin をリロード → **⚡同期**（予想テーブルは触らないので28件の予想は無事）
3. 完了後、蘭が本番を実測：年齢・異名・文章プロフィール・中止表示が全部乗ったか

> **静的JSだけの変更（決まり手リスト・一致割れ色・常時公開・中止カードUI）は push済みで既に本番反映**。
> DB同期が要るのは「選手プロフィール（年齢/文章）」と「中止フラグ」だけ。

### 今セッションで入れた機能（すべてpush済み）
- 名鑑：階級別＋王者最上段＋検索、年齢（生年月日で保持）、超RIZIN.5含む出場勢の文章プロフィール
- みんなの予想：受付中から常時公開（各カード下＋ALL PICKSマトリクス）
- 一致/割れの色分け（金＝全員一致／赤＝割れ）
- 決まり手リスト拡充（肩固めほか、実データ照合でKO13/一本15種）
- 試合の中止・延期（cancelled）＝大きなCANCELLED・予想は閲覧可・採点除外

### 残タスク（一次ソースが無く空欄のまま＝正当）
細川一颯・AJマッキー・ドンマイ川端（DATA MMAに戦績なし）／水野新太（公式写真なし）

### 次にやると言っていたこと
- **キャッチアップルーチン**（下記「## キャッチアップルーチン（設計案）」）
- HAKUの堀（Moat）章・アーキ付録を事業計画書へ（任意）
- ケイト vs NOEL を LANDMARK 16 のカードとして登録（延期先）

---

## 最重要の前提

### 1. `site/js/demo-data.js` が正本
Supabaseは**同期先**であって正本ではない。データを変えるときは必ず demo-data.js を編集し、下記の同期ボタンで反映する。

### 2. 反映は admin の同期ボタンから
1. `admin.html` を開く → Gakuのメール＋パスワードでログイン
2. **必ずページをリロードしてから**（古いJSが動いて反映されない事故が実際に起きた）
3. 「⚡ ローカルデータをSupabaseへ同期」を押す
4. 結果表示に「fighters: 40件」等が出れば成功

> Supabaseダッシュボード（SQL Editor）は**Chrome翻訳干渉＋高負荷で頻繁に無反応**になる。150秒待っても真っ黒のことがある。DDL以外はこのボタンで済ませる。

### 3. 決定打ルール（★このルールの正本はここ）
「あらます」の判定は**公式の決着表記ではなく、相手を倒した一撃**で行う。

> 右フックでダウン → パウンドでストップ　＝　決定打は **右フック**

MMAの公式表記は「TKO（パウンド）」が大半で、公式表記に合わせると`パウンド`ばかりが正解になり「あの右フックで倒れる」という読みが死ぬため。`パウンド（削り切り）`は殴り続けて削り切った試合だけに使う。
※ブックメーカーはKO/TKO・一本・判定までしか市場を作らない（技名は裁定が揉めるため）。**仲間内だから裁定できる**という前提の上に成り立つルール。

---

## ルール（二段梯子・2026-08-05に三段から簡素化）
1. **的中** — 勝者を当てる。ランキングの主役。分母は「結果が出た予想試合」だけ
2. **ピタリ** — 勝者＋**決まり手**まで一致。決まり手＝「判定」or 具体的な決定打（右フック／リアネイキドチョーク等）を1つ。
   - 判定決着 → 予想が「判定」でピタリ
   - KO/一本決着 → 予想の決定打(technique)が結果と一致でピタリ

勝者を外したら不成立。ドロー・無効試合は集計から除外。**ラウンド・旧「あらます」（決定打の別段）は廃止**。予想入力も「勝者→決まり手1つ」に簡素化（正本＝`app.js` の `scoreFight`＋`kimarite`）。

---

## 構成
```
site/
  index.html    プレイヤー選択＋大会一覧（カード0件の大会は自動でCOMING SOON）
  event.html    予想 ⇄ 答え合わせ（締切後はALL PICKSマトリクス）
  ranking.html  的中／ピタリ／あらます＋大会チェックボックス絞り込み
  fighter.html  選手名鑑（HIGHLIGHT・フィニッシュ内訳グラフ・戦績履歴）
  admin.html    Gaku専用：結果入力／選手編集／⚡同期／🏁結果発表
  js/config.js  Supabase接続情報（平文。publishable keyはRLS前提の公開キー）
  js/demo-data.js  ★正本
  assets/       lm15・r54（VSバナー/ポスター）、fighters（選手ポートレート）
supabase/schema.sql  ※実DBと差異あり（下記「既知の負債」参照）
```

### プレビュー
`.claude/launch.json` の `rizin-portal`（port 8765）。`/tmp/rizin-portal` にコピーして配信するため、**編集後は cp で同期**が必要（スキルの `bump_version.sh` が一括でやる）。

---

## 大会を追加・結果を入れるとき
**`rizin-card-intake` スキルを呼ぶこと**（`.claude/skills/rizin-card-intake/`）。
手作業でやり直すと同じ事故を踏む。スクリプト同梱：
- `fetch_event.py` — 大会ポスター＋VSバナー一式
- `fetch_fighters.py` — 公式ポートレート一括取得
- `bump_version.sh` — キャッシュバスター更新＋プレビュー同期
- `verify.js` — 整合性を一括検証（ブラウザに貼る）

---

## 外部予想の取り込み（NotebookLM → ポータル）※2026-08-04新設
インフルエンサー・各選手の勝敗予想を取り込み、**的中率ランキングの母集団を厚くする**（事業ビジョン＝オッズの主場化の布石）。スキル＝`rizin-prediction-intake`。

パイプライン：
```
YouTube予想動画 → NotebookLMにソース追加＋prompts/notebooklm_prediction_extract.md を貼る
  → 構造化出力ブロック → TONYに貼る → ingest_predictions.py で機械照合＋member化
  → admin コンソールに upsertスニペットを貼る → 本番へ即反映 → influencer_sources.md に出典記録
```
- **データモデル**：各予想者を `members` に1人足す（`m_yt_<slug>`）。既存の予想/ランキング機構をそのまま流用。将来「一般ユーザー予想」も同じ器
- **出典必須**：仲間内・noindex なら載せてOK。ただし各取り込みにチャンネル名＋動画URL＋投稿日を紐づける（`influencer_sources.md`）。**出典を示せない予想は載せない**
- **設計の芯**：選手名をLLMに要約させない（`ingest_predictions.py` が文字列照合）。`不明`は埋めない。技名は `techniques.js` の id だけ
- **プロンプト正本**：`prompts/notebooklm_prediction_extract.md`（出力フォーマットを規定・スクリプトと対で直す）
- **自動化ロードマップ**：v1手動→v2 admin取込欄→v3 字幕API（NotebookLM不要）→v4 catchup統合で無人化

## 既知の負債（次セッションで対応）

### A. バグ・負債
1. ~~`supabase/schema.sql` の写真3列欠落~~ **解消（2026-07-20）** — `highlight_url` / `highlight_caption` / `photo_casual` を追加。死に列 `photo_url` を削除。`stance` は元から入っていた（前回READMEの記述誤り）。api.js の同期payloadと全テーブル全列を突き合わせ済み
2. ~~`demo-data.js` のUnicodeエスケープ1839箇所~~ **解消（2026-07-20）** — 全て実文字化。ブラウザで DEMO_SEED の件数（3/40/4/20）と描画・コンソールを検証済み
3. **死んだUI** — 本番モードで `resetDemo()` が空実装。adminの「デモデータを戻す」ボタンが無反応
4. **未参照アセット7点** — 削除済みの選手・試合の画像が残存

### B. 名鑑の未整備（方針：**次の出場選手だけ完璧にする**）
40名中の空欄：構え40／普段着写真40／異名25／所属17／戦績履歴15／フィニッシュ内訳23。
→ **RIZIN.54の18名を優先**して埋める。過去選手は再登場時でよい。
- 異名は **`scripts/verify_nicknames.py` で機械照合するまで埋めたことにしない**（省略禁止）。`data-mma.com/nickname` の生HTMLと文字列一致で突き合わせる
  - 創作事故は**3回**発生：太田忍「銀狼の処刑人」／クレベル・コイケ「RIZINの鬼神」（正＝柔術界の鬼神）／パッチー・ミックス「ザ・ウィザード」（正＝ノー・ラブ）。原因は毎回**WebFetchの要約が固有名詞を創作する**こと。目視では3回とも止められなかったので機械照合に切り替えた
  - 複数の異名を持つ選手がいる（ジョニー・ケース＝「ハリウッド」「非情の貴公子」）。**連結して1つにしない**
  - 「未収録」と出たら出所が示せないということ。**空欄に戻す**
- 戦績内訳は**勝数と一致するときだけ**グラフが出る仕様。中途半端な内訳は入れない
- **構えは情報源が存在しない**（日本のMMAサイトに掲載項目が無い）。試合を観てadminから入れる運用
- 憶測で埋めない。調べて無ければ空欄のままにする

### C. ポスター — **解消（2026-07-20）**
**Gakuは毎大会「ポスターのクリエイティブ」を見るのを楽しみにしている。** サムネはここが本体。

- 事故の原因：**og:image をポスターとして使っていた**。あれはSNS共有用の16:9クロップで、
  ロゴ＋日付だけのテキスト画像やVSバナーが入っている。ポスター本体ではない
- 本物は**大会情報／チケットページ**に縦長（B2＝比0.707）で置かれている
- `fetch_event.py` を縦横比で選ぶ方式に変更済み（実走で1280×1809を自力取得できることを確認）
- 表示側：ポスターは大会ごとに縦横比が違う（B2縦・16:9横が混在）ため、
  `.eposter` で**切り抜かず全体を表示**し、余白はポスター自身をぼかして敷いている

### D. 公開（GitHub Pages）— **完了（2026-07-20）**
**URL：https://43417664gg-pixel.github.io/rizin-bishokukai/**

- リポジトリ：`43417664gg-pixel/rizin-bishokukai`（public・無料枠のため）
- 配信方式：**GitHub Actions**（`.github/workflows/pages.yml`）。`site/` がサブディレクトリでブランチ配信（ルート or `/docs`）では出せないため
- **main へ push すれば自動デプロイ**。所要2〜3分
- 公開前チェック通過：全5ページ `noindex` ／`config.js` は publishable key のみ（secret無し）／assets 5.6MB
- 本番URLで検証済み：`IS_DEMO=false`（Supabase実接続）・members 3・events 4・RIZIN.54は9試合で受付中（締切 2026-08-11 00:00 JST）・コンソールエラーなし

無料枠のためリポジトリは公開になる。`config.js` の publishable key も公開されるが**RLS前提の設計通り**（secret keyは含まない）。RIZIN公式画像を含む点はGaku了承済み・全ページ `noindex` 済み。

---

## 王者（名鑑の最上段）
出典＝Wikipedia「RIZIN FIGHTING FEDERATION」王者一覧（※2026年7月現在）。**大会出場の有無に関わらず全階級を掲載する**（Gaku方針・2026-07-20）。

| 階級 | 王者 | 防衛 |
|---|---|---|
| ライトヘビー級 | **空位** | — |
| ライト級 | ルイス・グスタボ | 0 |
| フェザー級 | ラジャブアリ・シェイドゥラエフ | 3 |
| バンタム級 | ダニー・サバテロ | 2 |
| フライ級 | 神龍誠 | 0 |
| 女子スーパーアトム級 | **空位** | — |

- 階級の表示順は軽い順ではなく**男子 → 女子 → 契約体重**（Gaku方針・2026-07-20）。正本＝`app.js` の `WEIGHT_ORDER`
- 王者判定は `isChampion()`＝`belt` に「RIZIN」かつ「王者/王座」。五輪メダルや他団体の過去王座は含めない
- 出場歴の無い王者は試合から階級を引けないため、`weightFromBelt()` が `belt` の「RIZINフェザー級王者」から階級を取る（**列を増やさない**＝新列は同期で黙って落ちるため）
- **王座が動いたら `belt` を更新すること**（自動追随しない）

## 認証情報
- Supabase URL / publishable key … `site/js/config.js`（平文・公開前提）
- admin ログイン … `43417664gg@gmail.com` ＋ **パスワードはGaku保持**（どこにも記録していない）
- RLS … 書き込みは上記メールでログインした場合のみ許可。匿名は読み取りと予想の投稿のみ

## 画像の出所
選手ポートレート・VSバナー・ポスターはすべてRIZIN公式（jp.rizinff.com）から取得。仲間内限定利用。
個人カメラマンのポートフォリオからは取得しない（保高幸子氏が「SNS外の利用はRIZIN広報に確認を」と本人明記のため）。

## キャッチアップルーチン（設計案・2026-08-03）
**目的**：TONY起動時（またはコマンド）に、ポータルをRIZINの最新状況へ自動追随させる。
MMAは「カード追加・中止/延期・欠場・結果・王座移動」が頻繁に動くため、手動だと必ず古くなる。

### 何を突き合わせるか（差分検出）
一次ソース（RIZIN公式／JMOC／DATA MMA）と demo-data.js を比較し、変化だけ拾う。

1. **新大会**：`_tags/大会情報` に、未登録の大会が出ていないか（新LANDMARK・超RIZIN等）→ あれば追加
2. **カード発表**：COMING SOON中の大会（LM16/17）の対戦カードが公開されたか → 出たら全試合登録（今回の超RIZIN.5と同じ）
3. **カード変更**：登録済み大会で、公式カードとの差分（**中止/延期・欠場・対戦相手変更・追加試合**）
   → ケイトのような中止は `cancelled` を立て、`cancel_note` に理由。差し替えはGaku判断を仰ぐ
4. **結果**：開催日を過ぎた大会（upcoming のまま）→ JMOC公式リザルトで結果入力・`finished` 化・各選手history追記
5. **王座移動**：Wikipedia王者一覧を再取得 → belt が変わっていれば更新（名鑑最上段に効く）
6. **プロフィール欠落**：次の出場選手で空欄があれば fetch_profiles / fetch_records で補完
7. **整合性**：`verify_nicknames.py` を実行（exit 0 まで異名は確定しない）

### 出力
「**変わったもの／取れたもの／要判断／要Gakuの手**」に分けて報告。
- 安全な追記（新カード・結果・プロフィール）は自動反映
- 判断が要るもの（中止理由・対戦相手変更）はGakuに確認
- DDLと⚡同期はGakuの手（毎度明示）

### 実装形態（案）
- `rizin-card-intake` スキルに **`catchup` モード**を足す or 専用スキル `rizin-catchup`
- 起動連携：ohayou（朝礼）から呼ぶ、または「RIZIN最新化」コマンドで発火
- スクリプトは既存を再利用（fetch_event / fetch_fighters / fetch_profiles / fetch_records / verify_nicknames）
- **差分検出ロジックだけ新規**（公式カード一覧 vs demo-data の集合比較）

### 注意（今セッションの教訓を継承）
- 固有名詞はLLMに要約させない（生HTML＋文字列照合）
- og:imageをポスターに使わない（縦横比で選ぶ）
- 列を足したら同期前にDDL（同期は無い列を黙って落とす）
- 「demo-data直した」は完了ではない（⚡同期まで届いて初めて仲間に見える）
